#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "pila.h"


int main() {
 Pila numeros;
 inicpila(&numeros);
 apilar(&numeros,5);
 desapilar(&numero);
 pilavacia(&numeros);
 tope(&numeros);
 leer (&numeros);
 mostrar(&numeros);
return 0;
}
